<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Edit <%= listing.name %></title>
    </head>
    <body>
        <%- include('../partials/_navbar.ejs') %>
        <h1>Edit a Listing!</h1>

        <form action="/listings/<%= listing._id %>?_method=PUT" method="POST">
            <label for="event">Event:</label>
            <input
                type="text"
                name="event"
                id="event"
                value="<%= listing.event %>"
            />
            <label for="description">Description:</label>
            <textarea name="description" id="description"><%= listing.description %></textarea>


            <label for="cause">Cause:</label>
            <select name="cause" id="cause">
                <option 
                    value="A"
                    <%=listing.cause === "A" ? "selected" : "" %>
                >
                    A
                </option>
				<option 
                    value="B"
                    <%=listing.cause === "B" ? "selected" : "" %>
                >
                    B
                </option>
				<option 
                    value="C"
                    <%=listing.cause === "C" ? "selected" : "" %>
                >
                    C
                </option>
				<option 
                    value="D"
                    <%=listing.cause === "D" ? "selected" : "" %>
                >
                    D
                </option>
            </select>    

            <label for="skills">Required Skills:</label>
            <textarea name="skills" id="skills"><%= listing.skills %></textarea>
            
            <label for="streetAddress">Street address:</label>
            <input
                type="text"
                name="streetAddress"
                id="street"
                value="<%= listing.streetAddress %>"
            />
            <label for="city">City:</label>
            <input
                type="text"
                name="city"
                id="city"
                value="<%= listing.city %>"
            />
            <label for="schedule">Event Schedule:</label>
            <input
                type="schedule"
                name="schedule"
                id="schedule"
                value="<%= listing.schedule %>"
            />

            <button type="submit">Edit Listing</button>
        </form>
    </body>
</html>