<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title><%= organization.orgName %>'s Profile</title>
    </head>
    <body>
        <%- include('../partials/_navbar.ejs') %>
        <a href="/organizations">Back to My Organizations</a>

        <h1><%= organization.orgName %></h1>

        <h2>Details</h2>
        <h3><%= organization.slogan %></h3>
        <p>Our Story: <%= organization.story %></p>
        <p>Cause: <%= organization.cause %></p>
        <p>Headquarters Address: <%= organization.headquarters %></p>
        <p>Email Address: <%= organization.email %></p>
        <p>Phone Number: <%= organization.phone %></p>

        <a href="/organizations/<%= organization._id %>/edit">Edit Profile</a>
        <form
            action="/organizations/<%= organization._id %>?_method=DELETE"
            method="POST"
        >
            <button type="submit">Delete Profile</button>
        </form>


        <h2>Listings</h2>
        <p>
            You own <%= myListings.length %> <%= myListings.length === 1 ?
            'listing.' : 'listings.' %>
        </p>
        <ul>
            <% myListings.forEach((listing) => { %>
            <li>
                A listing for the <%= listing.event %> event
                <a href="/listings/<%= listing._id %>">View more details.</a>
            </li>
            <% }) %>
        </ul>

        <a href="/listings/new/<%= organization._id %>">Add a New Listing</a>

    </body>
</html>