<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>My Organizations</title>
</head>

<body>
    <%- include('../partials/_navbar.ejs') %>
        <h1>Here's all my organizations:</h1>
        <ul>

			<% if (organizations.length > 0) {%>
				<% organizations.forEach((organization)=> { %>
					<% if (organization.owner._id.equals(currentUserId)) { %>	
						<li>
							<%= organization.orgName %>
							<a href="/organizations/<%= organization._id %>">View more details</a>
						</li>
					<% } %>
				<% }) %>
			<% } else { %>
				<li> No organizations found. </li>
			<% } %>

        </ul>

        <a href="/organizations/new">Add a New Organization</a>
</body>

</html>