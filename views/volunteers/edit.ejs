<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Edit Profile</title>
    </head>
    <body>
        <%- include('../partials/_navbar.ejs') %>
        <h1>Edit Volunteer Profile</h1>
        <form action="/volunteer/?_method=PUT" method="POST">
            <label for="title">Title:</label>
            <input
                type="text"
                name="title"
                id="title"
                value="<%= volunteer.title %>"
            />

            <label for="firstName">Fisrt Name:</label>
            <input type="text" name="firstName" id="firstName" value="<%= volunteer.firstName %>" />

            <label for="lastName">Last Name:</label>
            <input type="text" name="lastName" id="lastName" value="<%= volunteer.lastName %>" />

            <label for="cause">Cause:</label>
            <select name="cause" id="cause">
                <option 
                    value="A"
                    <%= volunteer.cause === "A" ? "selected" : "" %>                  
                >
                    A
                </option>
                <option 
                    value="B"
                    <%= volunteer.cause === "B" ? "selected" : "" %>                  
                >
                    B
                </option>
                <option 
                    value="C"
                    <%= volunteer.cause === "C" ? "selected" : "" %> 
                >
                    C
                </option>
                <option 
                    value="D"
                    <%= volunteer.cause === "D" ? "selected" : "" %>                   
                >
                    D
                </option>
            </select>    

            <label for="skills">Skills:</label>
            <input type="text" name="skills" id="skills" value="<%= volunteer.skills %>"/>

            <label for="model">Volunteering Model:</label>
            <select name="model" id="model">
                <option 
                    value="inPerson"
                    <%=volunteer.model === "inPerson" ? "selected" : "" %>
                >
                    In-Person
                </option>
                <option 
                    value="remote"
                    <%=volunteer.model === "remote" ? "selected" : "" %>
                >
                    Remote
                </option>
            </select>     

            <label for="availability">Availability:</label>
            <input type="text" name="availability" id="availability" value="<%= volunteer.availability %>" />

            <label for="location">Location:</label>
            <input type="text" name="location" id="location" value="<%= volunteer.location %>" />

            <label for="contactInfo">Contact Information:</label>
            <input type="text" name="contactInfo" id="contactInfo" value="<%= volunteer.contactInfo %>" />

            <label for="bio">Bio:</label>
            <textarea name="bio" id="bio"><%= volunteer.bio %></textarea>

            <label for="comments">Additional Comments:</label>
            <textarea name="comments" id="comments"><%= volunteer.comments %></textarea>

            <button type="submit">Update Profile</button>
        </form>
    </body>
</html>