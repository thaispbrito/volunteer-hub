<!DOCTYPE html>
<html lang="en">

    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <link rel="stylesheet" href="/stylesheets/style.css" />
        <title>My Profile</title>
    </head>

    <body>
        <%- include('../partials/_navbar.ejs') %>
        <h1>My Volunteer Profile</h1>
        <h3><%= volunteer.title %></h3>
        <p>Name: <%= volunteer.firstName %> <%= volunteer.lastName %></p>
        <p>Bio: <%= volunteer.bio %></p>
        <p>Cause: <%= volunteer.cause %></p>
        <p>Skills: <%= volunteer.skills %></p>
        <p>Volunteering Model: <%= volunteer.modelVol %></p>
        <p>Cause: <%= volunteer.cause %></p>
        <p>Availability: <%= volunteer.availability %></p>
        <p>Location: <%= volunteer.city %>, <%= volunteer.state %></p>
        <p>Email Address: <%= volunteer.email %></p>
        <p>Phone Number: <%= volunteer.phone %></p>
        <p>Additional Comments: <%= volunteer.comments %></p>

        <h3>Likes</h2>
        <p>You liked <%=myFavoriteListings.length%> listings</p>
        <ul>
            <% myFavoriteListings.forEach((listing) => { %>
            <li>
                A listing for the <%= listing.event %> event owned by <%=listing.owner.orgName %>.
                <a href="/listings/<%= listing._id %>">View more details.</a>
            </li>
            <% }) %>
        </ul>

        <a href="/volunteer/edit">Edit Profile</a>
        <form
            action="/volunteer/?_method=DELETE"
            method="POST"
        >
            <button type="submit">Delete Profile</button>
        </form>

    </body>

</html>